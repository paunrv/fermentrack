node_modules
dist
.next
.turbo
*.tsbuildinfo
# Environment — never commit secrets
.env
.env.local
.env*.local
**/.env.local
**/.env
!.env.example
!**/.env.example
.DS_Store

# The field database — one laptop, real data, never committed
proof/.data/
