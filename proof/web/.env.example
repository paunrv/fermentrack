# Local development against a plain Postgres cluster.
# Against Supabase this is the project's Postgres connection string.
DATABASE_URL=postgres://postgres@localhost:5433/proof_dev?host=/tmp

# Development only. Ignored unless the database says it is not production.
PROOF_DEV_USER=aldo@demo.example
