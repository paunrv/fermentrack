{
  "name": "@fermentrack/database",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "@fermentrack/typescript-config": "*",
    "prisma": "^5.22.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
